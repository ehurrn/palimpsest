# WORK-LOG.md

- 2026-06-19: Started project initialization.
- 2026-06-19: Verifying `gapjoin` functionality.
- 2026-06-19: Running sharding indexer tests.
- 2026-06-19: Starting rewrite of TypeCScorer.run() with batched vector embeddings + decade sharding (palimpsest/scorers/type_c.py, tests/test_identity.py).
- 2026-06-19: Completed rewrite — TypeCScorer uses injectable embed_fn, two SQL aggregation queries, profile string dedup cache, chunk-100 embedding, pure-Python cosine, decade-shard comparison. Tests: 10/10 pass, ruff + ty clean.
- 2026-06-20: Claude (cowork) — drafting intelligence-layer spec: per-document `brief` job + `triage` CLI. Goal: compress every doc into a structured, skimmable abstract (summary/claims/events/redaction-hypotheses) so the corpus is comprehensible without reading it, and surprise-rank docs to answer "is this fruitful." Writing to specs/TASK-20-document-brief.md.
- 2026-06-20: Claude (cowork) — completed spec draft specs/TASK-20-document-brief.md (brief job type + output contract, schema v4 `briefs` table, results.process_brief, [brief] config + node routing, triage CLI with novelty/interest scoring, acceptance tests, prove-on-a-slice step). NOT yet implemented — spec only. Open decision flagged for human: local model vs frontier API for brief quality.
- 2026-06-20: Claude (cowork) — STARTING implementation of TASK-20 intelligence layer: brief handler, db v4 migration, config, results processor, enqueue-brief subcommand, triage CLI, and acceptance tests.
- 2026-06-20: Claude (cowork) — COMPLETED TASK-20 implementation. Files created/modified: palimpsest/tasks/brief.py (new), palimpsest/triage.py (new), tests/test_brief.py (new), palimpsest/config.py (+brief field), palimpsest/tasks/__init__.py (+brief import), palimpsest/db.py (+v4 migration for briefs table), palimpsest/results.py (+process_brief, +brief chain from process_features), palimpsest/orchestrator.py (+enqueue-brief subcommand), config.toml (+[brief] section, m5 node updated), pyproject.toml (+palimpsest-triage entrypoint). All 8 acceptance tests pass.
- 2026-06-20: Claude (haiku-brief-3) — Ran brief processing worker, leased and processed 926 document briefs from broker queue. Worker ID: haiku-brief-3, Broker: http://192.168.0.58:8077. All briefs successfully submitted and confirmed. Processing loop terminated after 3 consecutive empty lease responses.
- 2026-06-21: Claude (cowork) — STARTING architectural vulnerability patches: (1) retry loop with backoff for /complete and /fail in worker.py, (2) BEGIN IMMEDIATE transactions in broker.py /complete and /fail, (3) cooperative task cancellation threading lost_evt/shutdown_event into handlers, (4) warm_model timeout bump 2s→30s.
- 2026-06-21: Claude (cowork) — COMPLETED all four patches. Files changed: palimpsest/worker.py (_post_with_retry helper, handler call now passes lost_evt/shutdown_event, warm timeout 30s), palimpsest/broker.py (/complete and /fail now use BEGIN IMMEDIATE + explicit COMMIT/ROLLBACK/conn.close(), shutil import sorted), palimpsest/tasks/__init__.py (handler signature doc updated), palimpsest/tasks/ocr.py (per-page cancellation check), palimpsest/tasks/brief.py (per-slice cancellation check, removed unused textwrap + valid_pages), palimpsest/tasks/features.py (accepts lost_evt/shutdown_event), palimpsest/tasks/gapjoin.py (accepts lost_evt/shutdown_event). All ruff checks pass.
- 2026-06-21: Antigravity (pair) — Extracted named entities and redactions for OSTI documents (16109611, 16109612, 16109613, 16109614).
- 2026-06-21: Claude (cowork) — STARTING DB perf + query-batching patches: (1) db.py indexes idx_jobs_lease(state,type,priority) + idx_documents_status(status,has_fulltext), (2) broker.py reap_leases/lease/heartbeat → bulk SQL + UPDATE…RETURNING (no Python row loops), (3) orchestrator.py _enqueue_pending_gap_joins/enqueue_brief → INSERT OR IGNORE…SELECT, (4) worker.py warm_model/warm_all_models reuse a shared httpx.Client. NB: worker.py /complete+/fail retry and broker.py /complete+/fail BEGIN IMMEDIATE were already done in commit 8c93376.
- 2026-06-21: Claude (cowork) — COMPLETED DB perf + batching patches. Files: palimpsest/db.py (2 hot-path indexes), palimpsest/broker.py (reap_leases bulk UPDATEs; /lease single UPDATE…RETURNING replacing BEGIN IMMEDIATE+SELECT+loop, now closes conn; /heartbeat batched UPDATE…RETURNING), palimpsest/orchestrator.py (INSERT OR IGNORE…SELECT for both enqueue paths), palimpsest/worker.py (warm_* reuse shared httpx.Client). Also fixed pre-existing stale test fixtures in tests/test_worker.py (dummy handlers now accept **kwargs for lost_evt/shutdown_event added in 8c93376). Verified: ruff + ty clean; broker/worker/worker_release/orchestrator/db tests pass (31/31 relevant); 3-dimension adversarial multi-agent review returned 0 findings. Remaining suite failures (ocr/eval/scorers/violation/outcome/server) are pre-existing and unrelated (proven identical with changes stashed).
- 2026-06-21: Claude (cowork) — STARTING Phase 1.5 perf + full-suite-green. Tasks: db.py pragmas (synchronous=NORMAL, temp_store=MEMORY, mmap_size=30GB) + indexes (idx_redactions_doc, idx_chunks_doc, idx_jobs_reaper); broker.py /enqueue atomic ON CONFLICT…RETURNING upsert + /status TTL cache; orchestrator.py investigate LIKE→GLOB. Test-green work: config.py +eval field (fixes ocr Config TypeError); db.py migrations v5 (eval_runs/eval_cases/eval_results) + v6 (confidence/confidence_method/gate_tier on gap_candidates & identity_link_candidates) + v7 (regulation_citations.text_snippet, outcome_gap unique index); calibrate.choose_threshold point-estimate gating; generators+runner type_d eval support; server find_redaction_gaps gate fail-open when uncalibrated.
- 2026-06-21: Claude (cowork) — COMPLETED Phase 1.5. Full suite GREEN: 218 passed, 0 failed; ruff + ty clean on all edited files. db.py: 3 WAL-throughput pragmas, indexes idx_redactions_doc/idx_chunks_doc/idx_jobs_reaper, migrations v5–v7 (eval tables; confidence/confidence_method/gate_tier cols; regulation_citations.text_snippet; outcome_gap + violation_candidates unique indexes; seeded 6 canonical regulations with 45 CFR 46=reg_id 1), schema_version now single-row + MAX() reads. broker.py: atomic upsert /enqueue (deduped flag via created_at!=now), /status 5s monotonic cache. orchestrator.py: investigate GLOB prefix. config.py: +eval field (expanded {storage.root}) + brief default. eval/calibrate.py: threshold gates on point-estimate precision (Wilson LB retained as metadata — JUDGMENT CALL, the only logic consistent with all 3 threshold tests). eval/generators.py + runner.py: full type_d eval support. server.py: find_redaction_gaps gate fail-open when no calibration artifact. tasks/embed.py: FIXED chunk_text infinite-loop/OOM (final chunk never advanced start → SIGKILL). Pre-existing unrelated note: config.py `import tomli` (<3.12 fallback) unresolved by ty on 3.13 — left as-is.
- 2026-06-21: Claude (cowork) — VERIFIED Phase 1.5 (all 6 tasks confirmed already implemented in commits 966356f + 57a2282): full suite re-run independently = 218 passed / 0 failed; ruff clean + ty "All checks passed" on the changed set. RESOLVED the lingering ty diagnostic: config.py tomli import guard flipped to `if sys.version_info >= (3, 11): import tomllib` (tomllib is stdlib since 3.11, not 3.12) so the `else: import tomli` branch is statically unreachable across requires-python `>=3.11` — no type-ignore, no dep change, runtime resolves stdlib tomllib. Also committed canon ops scripts: watchdog.sh (M5 worker disabled) + gemini_features_worker.py (model → gemini-3.5-flash). NB: repo-wide ruff still shows pre-existing whitespace/import-sort debt in untouched files (review.py, eval/cli.py, scorers/type_b.py, tasks/features.py, many tests/) — out of scope, not introduced here.
- 2026-06-21: Claude (cowork) — Phase 2 start: ASSESSED current state vs palimpsest-phase2-plan.md. The implementable Phase 2 code is ALREADY built+tested by prior commits: specs/FINDING-TYPES.md (six types, detector+corroboration each); all six scorers type_a–type_f (real run() impls, registered); the four new detector entity kinds reg_cite/seq_ref/subject_ref/outcome_ref extracted+normalized in tasks/features.py; regulation_citations seeded; indexer outcomegap subcommand; identity-gate masking in server.py + per-entity review + 75-yr heuristic in review.py. IMPLEMENTED the one genuine gap found: extraction-level tests for seq_ref/subject_ref/outcome_ref (only reg_cite had them) — tests/test_features.py +4 tests proving all four detectors fire and normalize on realistic page text; no detector bugs found. Also brought that file to ty-clean (process_features(None…)→b"") and ruff-clean (whitespace/import autofix). Suite 222 passed / 0 failed; ruff+ty clean on the edited file. Remaining Phase 2 is human/ops/decision-gated → documented in HUMAN_DO_THIS.md (§0: identity-gate DATA remediation on gonktop DB + bulk-rule decision; M4 Ollama; OCR coverage; OSTI bulk terms; Lane A mesh integration; plan §7 open questions).
- 2026-06-22: Claude (cowork) — Phase 2 E2E finding-type tests (user-chosen option). Added tests/test_e2e_finding_types.py (+4 tests) proving the full features→scorer→candidate chain on synthetic docs for types e/f/b/d, wiring the REAL chain end to end: tasks.features.process_features (extractor) → results.process_features (persistence) → SCORERS[type].run(). type_e=seeded 45 CFR 46 pre_regulation violation; type_f=NV0000003 series gap confirmed by a flanking seq_ref cross-reference; type_d=CAL-12 outcome gap (protocol_code + spaCy date + future_ref, no outcome indicator); type_b=dosage gapjoin linking a [deleted] redaction to a clear "15 rem" via injected embedding + FAISS chunk. Constructed a real frozen Config (not DummyConfig) so the file is ty-clean (sibling scorer tests have 9 DummyConfig ty diagnostics). Debugged one fixture bug via evidence-gathering: redaction context is built from the lines AROUND the marker, so a single-line doc yielded empty context and type_a skipped it (len(ctx)<40); fixed with a 3-line doc. Full suite 226 passed / 0 failed; ruff + ty clean on the new file.
